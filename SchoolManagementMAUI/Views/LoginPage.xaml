<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:SchoolManagementMAUI.ViewModels"
             x:Class="SchoolManagementMAUI.Views.LoginPage"
             Title="Login">

    <!--<ContentPage.BindingContext>
        <vm:LoginViewModel />
    </ContentPage.BindingContext>-->

    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">
            <!-- Logo or Title -->
            <VerticalStackLayout Spacing="10" HorizontalOptions="Center">
                <Label Text="Welcome" FontSize="28" FontAttributes="Bold" HorizontalOptions="Center" />
                <Label Text="Login to continue" FontSize="16" TextColor="Gray" HorizontalOptions="Center" />
            </VerticalStackLayout>

            <!-- Login Fields -->
            <VerticalStackLayout Spacing="15">
                <Entry Placeholder="Email" 
                       Text="{Binding Email}" 
                       Keyboard="Email" 
                       Margin="0,0,0,10" />
                <Entry Placeholder="Password" 
                       Text="{Binding Password}" 
                       IsPassword="True" 
                       Margin="0,0,0,10" />

                <!-- Login Button -->
                <Button Text="Login" 
                        Command="{Binding LoginCommand}" 
                        BackgroundColor="#007AFF"
                        TextColor="White"
                        CornerRadius="8"
                        HeightRequest="50"
                        Margin="0,10,0,0" />
            </VerticalStackLayout>

            <!-- Password Recovery Section -->
            <VerticalStackLayout Spacing="10">
                <Label Text="Forgot your password?" 
                       FontSize="14" 
                       HorizontalOptions="Center" 
                       TextColor="Gray" />

                <Button Text="Recover Password" 
                        Command="{Binding GoToRecoverPasswordCommand}" 
                        BackgroundColor="Transparent"
                        TextColor="#007AFF"
                        BorderColor="#007AFF"
                        BorderWidth="1"
                        CornerRadius="8"
                        HeightRequest="40" />




            </VerticalStackLayout>

            <!-- Status Message -->
            <Label Text="{Binding Message}" 
                   TextColor="Blue" 
                   HorizontalOptions="Center" 
                   HorizontalTextAlignment="Center"
                   IsVisible="{Binding HasMessage}" />
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>