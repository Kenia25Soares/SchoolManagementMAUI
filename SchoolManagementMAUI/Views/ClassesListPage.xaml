<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolManagementMAUI.Views.ClassesListPage"
             Title="Classes">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Back" Clicked="OnBackClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">
            <CollectionView ItemsSource="{Binding CoursesWithClasses}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Spacing="4">
                            <!-- Course Header -->
                            <Frame Style="{StaticResource EducationalCourseHeaderFrame}" BackgroundColor="#28A745">
                                <Label Text="{Binding Course.Name}" Style="{StaticResource EducationalCourseHeaderLabel}"/>
                            </Frame>

                            <!-- Classes List -->
                            <CollectionView ItemsSource="{Binding Classes}" Margin="16,0,0,8">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame Style="{StaticResource EducationalClassItemFrame}">
                                            <VerticalStackLayout>
                                                <Label Text="{Binding Name}" Style="{StaticResource EducationalClassNameLabel}"/>
                                                <Label Text="{Binding AcademicYear}" Style="{StaticResource EducationalClassDetailLabel}"/>
                                                <Label Text="{Binding Shift}" Style="{StaticResource EducationalClassDetailLabel}"/>
                                            </VerticalStackLayout>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>