<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SchoolManagementMAUI.Views.CoursesListPage"
             Title="Courses">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Back" Clicked="OnBackClicked" />
    </ContentPage.ToolbarItems>

    <ScrollView>
        <VerticalStackLayout Padding="16" Spacing="12">
            <CollectionView ItemsSource="{Binding CoursesWithSubjects}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <VerticalStackLayout Spacing="4">
                            <!-- Course Header -->
                            <Frame Style="{StaticResource EducationalCourseHeaderFrame}" BackgroundColor="{StaticResource EducationalBlue}">
                                <Label Text="{Binding Course.Name}" Style="{StaticResource EducationalCourseHeaderLabel}"/>
                            </Frame>

                            <!-- Subjects List -->
                            <CollectionView ItemsSource="{Binding Subjects}" Margin="16,0,0,8">
                                <CollectionView.ItemTemplate>
                                    <DataTemplate>
                                        <Frame Style="{StaticResource EducationalSubjectItemFrame}" BackgroundColor="#F0F0F0">
                                            <Label Text="{Binding Name}" Style="{StaticResource EducationalSubjectItemLabel}"/>
                                        </Frame>
                                    </DataTemplate>
                                </CollectionView.ItemTemplate>
                            </CollectionView>
                        </VerticalStackLayout>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </VerticalStackLayout>
    </ScrollView>

</ContentPage>